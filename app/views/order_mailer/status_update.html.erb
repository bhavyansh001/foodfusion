<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= t('email.order_status.subject') %></title>
</head>
<body style="font-family: Arial, sans-serif; line-height: 1.6; color: #333333; max-width: 600px; margin: 0 auto; padding: 20px;">
    <div style="background-color: #f8f8f8; border-radius: 10px; padding: 20px; margin-bottom: 20px;">
        <h1 style="color: #4a5568; text-align: center; margin-bottom: 20px;"><%= t('email.order_status.subject') %></h1>
        
        <p style="font-size: 16px;"><%= t('email.order_status.greeting', email: @order.visitor.email) %></p>
        
        <p style="font-size: 16px; margin-bottom: 20px;"><%= t('email.order_status.updated_to', order_id: @order.id) %></p>
        
        <div style="background-color: #ebf8ff; border-left: 4px solid #4299e1; padding: 10px; margin-bottom: 20px;">
            <p style="font-size: 18px; font-weight: bold; color: #2b6cb0; margin: 0;"><%= @order.status.titleize %></p>
        </div>
        
        <% case @order.status %>
        <% when 'confirmed' %>
            <p style="font-size: 16px;"><%= t('email.order_status.confirmed') %></p>
        <% when 'in_progress' %>
            <p style="font-size: 16px;"><%= t('email.order_status.in_progress') %></p>
        <% when 'completed' %>
            <p style="font-size: 16px;"><%= t('email.order_status.completed') %></p>
        <% when 'cancelled' %>
            <p style="font-size: 16px;"><%= t('email.order_status.cancelled') %></p>
        <% end %>
        
        <p style="font-size: 16px; margin-top: 20px;"><%= t('email.order_status.thank_you') %></p>
    </div>
    
    <div style="text-align: center; font-size: 14px; color: #718096;">
        <p><%= t('email.order_status.contact_us') %></p>
        <p><%= t('email.order_status.footer', year: Time.now.year, restaurant_name: @order.restaurant.name) %></p>
    </div>
</body>
</html>
