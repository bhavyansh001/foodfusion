<div id="order_<%= order.id %>_status" class="mb-4">
  <span class="font-semibold">Status:</span>
  <span class="px-2 py-1 rounded-full text-sm <%= order_status_color(order.status) %>">
    <%= order.status.capitalize %>
  </span>
</div>