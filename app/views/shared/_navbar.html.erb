<nav class="bg-white shadow-lg" data-controller="navbar">
  <div class="max-w-6xl mx-auto px-4">
    <div class="flex justify-between">
      <div class="flex space-x-7">
        <div>
          <%= link_to root_path, class: "flex items-center py-4 px-2" do %>
            <span class="font-semibold text-gray-500 text-lg">FoodFusion</span>
          <% end %>
        </div>
      </div>
      <!-- Primary Navbar items -->
      <div class="hidden md:flex items-center space-x-1">
        <%= link_to t('home.home'), root_path, class: "nav-link" %>
        <% if user_signed_in? %>
          <%= link_to t('home.your_profile'), visitor_dashboard_path, class: "nav-link" %>
          <% if current_user.owner? %>
            <%= link_to t('home.manage_restaurant'), owner_dashboard_index_path, class: "nav-link" %>
          <% end %>
          <%= link_to t('home.sign_out'), destroy_user_session_path, data: { turbo_method: :delete }, class: "nav-link" %>
        <% else %>
          <%= link_to t('home.log_in'), new_user_session_path, class: "nav-link" %>
          <%= link_to t('home.sign_up'), new_user_registration_path, class: "nav-link" %>
        <% end %>
      </div>
      <!-- Mobile menu button -->
      <div class="md:hidden flex items-center">
        <button class="outline-none mobile-menu-button" data-action="click->navbar#toggleMobileMenu">
          <svg class="w-6 h-6 text-gray-500 hover:text-blue-500"
               fill="none"
               stroke-linecap="round"
               stroke-linejoin="round"
               stroke-width="2"
               viewBox="0 0 24 24"
               stroke="currentColor"
          >
            <path d="M4 6h16M4 12h16M4 18h16"></path>
          </svg>
        </button>
      </div>
    </div>
  </div>
  <!-- mobile menu -->
  <div class="hidden mobile-menu" data-navbar-target="mobileMenu">
    <ul class="">
      <li><%= link_to t('home.home'), root_path, class: "mobile-nav-link" %></li>
      <% if user_signed_in? %>
        <li><%= link_to t('home.your_profile'), visitor_dashboard_path, class: "mobile-nav-link" %></li>
        <% if current_user.owner? %>
          <li><%= link_to t('home.manage_restaurant'), owner_dashboard_index_path, class: "mobile-nav-link" %></li>
        <% end %>
        <li><%= link_to t('home.sign_out'), destroy_user_session_path, data: { turbo_method: :delete }, class: "mobile-nav-link" %></li>
      <% else %>
        <li><%= link_to t('home.log_in'), new_user_session_path, class: "mobile-nav-link" %></li>
        <li><%= link_to t('home.sign_up'), new_user_registration_path, class: "mobile-nav-link" %></li>
      <% end %>
    </ul>
  </div>
</nav>