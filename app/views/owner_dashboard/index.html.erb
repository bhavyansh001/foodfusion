<div>
  <h1 class="font-bold text-4xl">OwnerDashboard</h1>
  <% @restaurants.each do |restaurant| %>
  <div>
     <h2><%= link_to restaurant.name, dashboard_path(restaurant) %></h2>
  </div>
<% end %>
 <%= link_to "Add New Restaurant", new_restaurant_path %>
</div>
